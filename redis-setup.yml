- hosts: all
  become: yes
  vars:
    ansible_connection: community.aws.aws_ssm
    ansible_aws_ssm_region: us-east-1
    ansible_aws_ssm_bucket_name: "ssm-session-logs-{{ ansible_aws_ssm_region }}"
  tasks:
    - include_tasks: roles/tasks/main.yml
